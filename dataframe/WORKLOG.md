# 作業ログ - DataFrameライブラリ開発

## 2026年2月3日 13:36:41

### プロジェクト概要
- 場所: `C:\Users\user\sync_project\20260203\dataframe\`
- 目的: pandasのようなDataFrameライブラリを自作

### 開発の経緯
1. ユーザーから「dataframeを自作してほしい」というリクエスト
2. 作業フォルダに`dataframe/`ディレクトリを作成
3. コア機能を実装

### 実装した機能
- DataFrame: 2次元ラベル付きデータ構造
- Series: 1次元ラベル付き配列
- データ選択: iloc, filter, head, tail
- データ操作: sort_values, drop, rename, apply
- GroupBy: groupby + 集計（sum, mean, min, max, count）
- 結合: join（inner, left, right, outer）
- 連結: concat（行/列）
- I/O: CSV, Parquet

### ソースコード規模

| ファイル | 行数 | 説明 |
|---------|------|------|
| core.py | 476行 | DataFrameクラス（本体） |
| operations.py | 330行 | GroupBy, join, concat |
| series.py | 136行 | Seriesクラス |
| utils.py | 50行 | ユーティリティ |
| **合計** | **約1,019行** | 主要コードのみ |

### テスト状況
- テスト数: 30個（Series: 7, DataFrame: 10, GroupBy: 3, Join: 2, Concat: 2, CSV: 1, Parquet: 1）
- 結果: すべてパス

### 10万行ベンチマーク結果（自作DataFrameのみ）

| 操作 | 時間 | 備考 |
|------|------|------|
| DataFrame作成 | 0.005秒 | - |
| CSV書き出し | 0.866秒 | 5.09 MB |
| Parquet書き出し | 1.820秒 | 2.82 MB |
| CSV読み込み | 2.546秒 | - |
| Parquet読み込み | 1.022秒 | - |
| フィルタ (age>50) | 0.421秒 | 47,606行 |
| ソート (salary) | 0.676秒 | - |
| GroupBy (department) | 0.280秒 | - |

### 気づき・発見
- **驚き**: わずか約1,000行でDataFrameの基本機能が実装できた
- **実用性**: 10万行のデータ処理が問題なく実行可能
- **Parquet優位**: 読み込みはParquetがCSVの2.5倍高速
- **圧縮率**: ParquetはCSVの1.8倍小さい

---

## 2026年2月3日 13:38:04

### pandasとの比較ベンチマーク実施

同じ10万行のデータで、自作DataFrameとpandasを同じ条件で比較

| 操作 | 自作DataFrame | pandas | 比率的 | 勝者 |
|------|--------------|--------|--------|------|
| **DataFrame作成** | 0.005秒 | 0.295秒 | 0.02x | **自作 (59倍速い)** |
| **フィルタ (age>50)** | 0.290秒 | 0.012秒 | 23.5x | **pandas (24倍速い)** |
| **ソート (salary)** | 0.585秒 | 0.024秒 | 24.8x | **pandas (25倍速い)** |
| **GroupBy (department)** | 0.173秒 | 0.023秒 | 7.6x | **pandas (8倍速い)** |
| **列選択** | 0.007秒 | 0.005秒 | 1.3x | 同等 |
| **head(100)** | 0.002秒 | 0.001秒 | 1.5x | 同等 |
| **総合計** | **1.061秒** | **0.360秒** | 2.9x | **pandas (約3倍速い)** |

### 気づき・発見（比較から）

1. **自作DataFrameの強み**:
   - 作成は驚くほど速い（pandasの59倍）
   - 純粋なPython実装なのでオーバーヘッドが少ない

2. **pandasの強み**:
   - データ操作（フィルタ、ソート、GroupBy）は圧倒的に速い
   - C/C++で最適化された内部処理の威力
   - NumPyのベクトル化演算の恩恵

3. **総合的な見解**:
   - 自作DataFrameは「学習用」としては十分な性能
   - 実務での大規模データ処理にはpandasが圧倒的に有利
   - しかし、1,000行のコードでここまで動くのは驚異的

### 今後の改善案（もしやるなら）
- NumPyを活用してベクトル化演算を実装
- インデックス参照を高速化（辞書検索の最適化）
- GroupByのアルゴリズム改善

---

## 2026年2月3日 13:54:36

### 100万行での比較ベンチマーク実施

データ量を10倍に増やして再比較

| 操作 | 自作DataFrame | pandas | 比 | 勝者 |
|------|--------------|--------|-----|------|
| **作成** | **0.042秒** | 3.832秒 | 0.01x | 🏆 **自作 (91倍速い)** |
| **フィルタ (age>50)** | 3.868秒 | **0.065秒** | 59.8x | 🏆 **pandas (60倍速い)** |
| **ソート (salary)** | 7.321秒 | **0.653秒** | 11.2x | 🏆 **pandas (11倍速い)** |
| **GroupBy (department)** | 1.990秒 | **0.180秒** | 11.1x | 🏆 **pandas (11倍速い)** |
| **総合計** | **13.221秒** | **4.729秒** | 2.8x | 🏆 **pandas (2.8倍速い)** |

### 気づき・発見（100万行から）

1. **作成速度の差が拡大**:
   - 10万行: 59倍
   - 100万行: **91倍**
   - データ量が増えるほど自作DataFrameの作成速度優位が拡大

2. **データ操作の差も拡大**:
   - フィルタ: 24倍 → **60倍** に悪化
   - ソート: 25倍 → **11倍** に改善
   - GroupBy: 8倍 → **11倍** に悪化

3. **総合的な見解**:
   - 純粋なPythonリストのソートは予想以上に健闘
   - フィルタ処理がボトルネック（ラムダ関数呼び出しのオーバーヘッド？）
   - GroupByも改善の余地あり

### 高速化の優先順位（仮説）

1. **フィルタ処理** - 60倍の差が最大のボトルネック
   - ラムダ関数ではなく、直接比較を可能にする設計
   - 例: `df[df['age'] > 50]` 形式の実装

2. **GroupBy** - 11倍の差
   - ハッシュテーブルの実装見直し
   - 辞書操作の最適化

3. **ソート** - 11倍の差
   - PythonのTimsortは優秀なので、ここは悪くない

---

## 2026年2月3日 18:16:07

### フィルタ処理高速化実装

ブールインデックス参照 `df[df['age'] > 50]` を実装

#### 実装内容
- `_ColumnProxy`クラスを追加
- 比較演算子（`>`, `<`, `==`, `>=`, `<=`, `!=`）をサポート
- 論理演算子（`&`, `|`, `~`）をサポート
- `__getitem__`でブール配列を受け取れるように拡張

#### 高速化結果（100万行）

| 方法 | 時間 | 対pandas | 対旧方法 |
|------|------|----------|----------|
| 旧方法 (lambda) | 2.093秒 | 110.8倍 | - |
| **新方法** (boolean) | **1.115秒** | 59.0倍 | **1.9倍速い** |
| pandas | 0.019秒 | - | 110.8倍速い |

#### 使用方法
```python
# 旧方法（遅い）
df.filter(lambda row: row['age'] > 50)

# 新方法（速い）
df[df['age'] > 50]

# 複数条件
df[(df['age'] > 50) & (df['salary'] < 100000)]
df[(df['department'] == 'Sales') | (df['department'] == 'HR')]
```

#### 気づき・発見
- **効果**: 旧方法より1.9倍高速化
- **課題**: まだpandasと59倍の差がある
- **原因**: 純粋なPythonリスト内包表記の限界
- **次のステップ**: NumPyを使ったベクトル化が必要か？

---

## 2026年2月3日 18:18:38

### 1000万行での比較ベンチマーク実施（フィルタのみ）

データ量をさらに10倍に増やしてテスト

| 操作 | 自作DataFrame | pandas | 比 | 勝者 |
|------|--------------|--------|-----|------|
| **作成** | **0.147秒** | 18.228秒 | 0.01x | 🏆 **自作 (124倍速い)** |
| **フィルタ (age>50)** | 8.762秒 | **0.241秒** | 36.4x | 🏆 **pandas (36倍速い)** |

### データ量による性能推移

| 行数 | 作成比（優位） | フィルタ比 |
|------|---------------|-----------|
| 10万行 | 59倍 | 24倍 |
| 100万行 | 91倍 | 60倍 |
| **1000万行** | **124倍** | **36倍** |

### 気づき・発見（1000万行から）

1. **作成速度の優位性が拡大**:
   - 59倍 → 91倍 → **124倍**
   - データ量が増えるほど自作DataFrameが圧倒的に速い
   - pandasはデータ作成時に多くのオーバーヘッドがある

2. **フィルタ処理の比率が改善**:
   - 100万行: 60倍
   - 1000万行: **36倍** ← 改善！
   - 高速化の効果がデータ量が大きいほど顕著

3. **実用性の目安**:
   - 1000万行のフィルタ: 8.76秒 → 実用範囲内
   - 作成は一瞬（0.15秒）
   - pandasへの移行もスムーズ

---

## 2026年2月3日 23:45:26

### GroupBy高速化（緊急対応）

#### 問題発覚
GroupByが致命的に遅いことが判明：

| 行数 | 元の実装 | スケール |
|------|---------|---------|
| 1万行 | 0.9秒 | - |
| 5万行 | 43秒 | データ5倍→時間45倍 ⚠️ |
| 10万行 | 196秒（3.3分） | データ2倍→時間4.6倍 ⚠️ |
| 1000万行（推定） | **5.4時間** | **O(n²)の問題** |

#### 原因
1. `self._df[self._by][i]` - 毎回`_ColumnProxy`経由でアクセス
2. `self._df[col][i]` in agg - 同じ問題
3. `if key not in groups: groups[key] = []` - 毎回チェック

#### 対策
1. 直接`_data`にアクセス：`self._df._data[self._by][i]`
2. `defaultdict`を使用してチェックを省略
3. `enumerate`を使用してインデックス取得を最適化

#### 高速化結果

| 行数 | 元の実装 | 最適化後 | 高速化 |
|------|---------|---------|--------|
| 1万行 | 0.9秒 | **0.005秒** | 180倍 |
| 10万行 | 196秒 | **0.074秒** | **2,635倍** 🎉 |
| 100万行 | 推定33分 | **0.76秒** | **2,600倍** |
| 1000万行（推定） | 5.4時間 | **7.6秒** | **約2,500倍** |

#### スケーリング検証（最適化後）

| 行数 | 時間 | スケール |
|------|------|---------|
| 1万行 | 0.008秒 | - |
| 10万行 | 0.073秒 | 9倍 ✅ 線形 |
| 100万行 | 0.759秒 | 10倍 ✅ 線形 |
| 1000万行 | 7.6秒（推定） | 10倍 ✅ 線形 |

**O(n²) → O(n) に改善！**

#### 実装変更
```python
# Before
for i in range(len(self._df)):
    key = self._df[self._by][i]  # Slow: __getitem__ -> ColumnProxy
    if key not in groups:
        groups[key] = []
    groups[key].append(i)

# After
groups = defaultdict(list)
column_data = self._df._data[self._by]  # Direct access
for i, key in enumerate(column_data):
    groups[key].append(i)  # No check needed
```

#### 気づき・発見
- **効果**: 10万行が3.3分 → 0.07秒（2,635倍）
- **スケール**: O(n²)からO(n)に改善
- **実用性**: 1000万行のGroupByが5.4時間から7.6秒に
- **教訓**: `__getitem__`のオーバーヘッドは大きい
